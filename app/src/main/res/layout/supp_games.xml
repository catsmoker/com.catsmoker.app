<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="?attr/colorSurface"
    android:orientation="vertical"
    android:paddingHorizontal="16dp"
    android:paddingTop="16dp">

    <!--
         Search Bar Wrapper
         Wrapped in a Card to create the "Floating Search Bar" look of Material 3
    -->
    <com.google.android.material.card.MaterialCardView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginBottom="16dp"
        app:cardBackgroundColor="?attr/colorSurfaceContainerHigh"
        app:cardCornerRadius="24dp"
        app:cardElevation="0dp"
        app:strokeWidth="0dp">

        <!--
            android:queryBackground="@null" removes the old-school underline
            theme="..." ensures text color adapts to background
        -->
        <SearchView
            android:id="@+id/search_view_games"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:iconifiedByDefault="false"
            android:queryBackground="@null"
            android:queryHint="@string/search_games_hint"
            android:theme="@style/ThemeOverlay.AppCompat.DayNight.ActionBar" />
    </com.google.android.material.card.MaterialCardView>

    <!-- List View -->
    <ListView
        android:id="@+id/list_view_games"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"
        android:background="@android:color/transparent"
        android:clipToPadding="false"
        android:divider="@android:color/transparent"
        android:dividerHeight="4dp"
        android:fadeScrollbars="false"
        android:paddingBottom="16dp"
        android:scrollbars="vertical" />

</LinearLayout>